'use client';

import { Database, FileCheck2, MessageSquare, RotateCcw, ShieldCheck } from 'lucide-react';
import { useDemo } from '../demo/demo-provider';

const statusGroups = { Programs: ['draft', 'planning', 'registration-open', 'full', 'active', 'completed', 'canceled', 'archived'], Registrations: ['incomplete', 'confirmed', 'waitlisted', 'offer-sent', 'canceled', 'completed', 'refunded'], Tasks: ['not-started', 'in-progress', 'awaiting-reply', 'blocked', 'done', 'canceled'] };

export function SettingsView() { const { resetDemoData } = useDemo(); return <div className="staff-page"><div className="staff-page-heading"><div><p className="staff-kicker">Workspace controls</p><h1>Settings</h1><p>Controlled dictionaries and demo boundaries, kept intentionally small.</p></div><button className="staff-button staff-button-outline" type="button" onClick={() => resetDemoData()}><RotateCcw size={16} /> Reset demo</button></div><div className="settings-grid"><section className="staff-panel"><div className="staff-panel-heading"><div><span className="staff-kicker">Status dictionaries</span><h2>Shared language</h2></div><ShieldCheck size={18} /></div>{Object.entries(statusGroups).map(([group, values]) => <div className="settings-group" key={group}><h3>{group}</h3><div className="status-chip-wrap">{values.map((value) => <span className="settings-chip" key={value}>{value.replaceAll('-', ' ')}</span>)}</div></div>)}</section><section className="staff-panel"><div className="staff-panel-heading"><div><span className="staff-kicker">Demo forms</span><h2>Versions</h2></div><FileCheck2 size={18} /></div><div className="settings-list"><div><b>Participation waiver</b><span>Version 2026.1 · fictional</span></div><div><b>Photo & video choice</b><span>Version 2026.1 · separate yes/no</span></div><div><b>Pickup policy</b><span>Version 2026.1 · fictional</span></div></div><div className="staff-panel-heading settings-subheading"><div><span className="staff-kicker">Message templates</span><h2>Mock only</h2></div><MessageSquare size={17} /></div><div className="settings-list"><div><b>Arrival reminder</b><span>Simulated in program communications</span></div><div><b>Registration confirmation</b><span>Shown in local confirmation state</span></div></div></section><section className="staff-panel"><div className="staff-panel-heading"><div><span className="staff-kicker">Roles</span><h2>Demo profiles</h2></div></div><div className="role-list"><div><b>Family</b><span>View and edit the fictional family’s registrations, forms, and account.</span></div><div><b>Staff</b><span>Manage the fictional operations desk, programs, partners, projects, and ledger.</span></div></div></section><section className="staff-panel"><div className="staff-panel-heading"><div><span className="staff-kicker">Storage boundary</span><h2>Browser only</h2></div><Database size={18} /></div><p className="settings-copy">This prototype uses <code>rookie-rackets:demo:v1</code> in localStorage after hydration. No records are transmitted. Reset clears the current walkthrough and restores the original fixtures; signing out clears only the demo session.</p><div className="demo-boundary">No database · no API routes · no authentication · no payments · no email · no Drive API</div></section></div></div>; }
